name: CI (PHP × Firebird Matrix)

on:
  push:
    branches:
      - '3.10.x'
      - '3.10.x-dev'
      - '3.0.*'
      - 'main'
      - 'feature/*'
  pull_request:
    branches:
      - '3.10.x'
      - '3.10.x-dev'
      - '3.0.*'
      - 'main'
  schedule:
    - cron: '0 2 * * 1'  # Weekly Monday 2 AM UTC
  workflow_dispatch: ~

# ==============================================================================
# PHP VERSION MANAGEMENT - READ BEFORE MODIFYING MATRIX
# ==============================================================================
# Before adding a PHP version to the matrix, VERIFY availability:
#
# 1. Check php.net/supported-versions.php for release status
# 2. Verify Docker image exists: docker manifest inspect php:X.Y-cli-bookworm
# 3. Test locally with: docker pull php:X.Y-cli-bookworm
#
# PHP Release Schedule (annual, November):
# - PHP 8.1: Nov 2021 - Security until Dec 2025 (EOL imminent)
# - PHP 8.2: Dec 2022 - Security until Dec 2026
# - PHP 8.3: Nov 2023 - Active until Dec 2025, Security until Dec 2027
# - PHP 8.4: Nov 2024 - Active until Dec 2026, Security until Dec 2028
# - PHP 8.5: Nov 2025 - Active until Dec 2027, Security until Dec 2029
# - PHP 8.6: Expected Nov 2026 (DO NOT ADD until Docker image available)
#
# Docker Hub PHP images are published AFTER GA release, not for alpha/beta/RC.
# ==============================================================================

jobs:
  test:
    name: PHP ${{ matrix.php-version }} / Firebird ${{ matrix.firebird.version }}
    runs-on: ubuntu-latest
    # Firebird 4.0/5.0 are experimental — php-firebird has known crashes with FB5.
    # Don't fail the overall CI for experimental targets.
    continue-on-error: ${{ matrix.firebird.experimental }}
    permissions:
      contents: read

    # Use service containers for Firebird instead of IBSurgeon scripts.
    # Official Firebird Docker images: https://hub.docker.com/r/firebirdsql/firebird
    services:
      firebird:
        image: firebirdsql/firebird:${{ matrix.firebird.docker-tag }}
        env:
          FIREBIRD_ROOT_PASSWORD: masterkey
          FIREBIRD_DATABASE: test.fdb
          FIREBIRD_DATABASE_DEFAULT_CHARSET: UTF8
          # Fix for Firebird 3.0/4.0 on some kernels
          FIREBIRD_RELAX_LOCK_SECURITY: "true"
          # Allow connections from any host (needed for service container networking)
          TZ: UTC
        ports:
          - 3050:3050
        # Health check uses fbsvcmgr (available in firebirdsql/firebird images)
        # Note: nc/netcat is NOT available in these Alpine-based images
        options: >-
          --health-cmd "/opt/firebird/bin/fbsvcmgr localhost:service_mgr user SYSDBA password masterkey info_server_version"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
          --health-start-period 30s

    strategy:
      fail-fast: false
      matrix:
        # PHP versions - must be released GA with Docker Hub image available
        # See header comment for verification steps before adding new versions
        # PHP 8.1/8.2 dropped: config.platform.php=8.3 means installed deps
        # may use PHP 8.3+ syntax. PHP 8.1 EOL Dec 2025.
        # Minimum effective PHP version is 8.3.
        php-version: ['8.3', '8.4', '8.5']
        # Firebird configs: [display-version, docker-tag, phpunit-config, experimental]
        # Firebird 3.0 is the primary target (Amicron ERP). FB4/FB5 are informational.
        # php-firebird has known SIGABRT crashes with FB5 — marked experimental.
        firebird:
          - version: '3.0'
            docker-tag: '3'
            config: 'phpunit.xml'
            experimental: false
          - version: '4.0'
            docker-tag: '4'
            config: 'phpunit-firebird4.xml'
            experimental: true
          - version: '5.0'
            docker-tag: '5'
            config: 'phpunit-firebird5.xml'
            experimental: true

    env:
      # Default Firebird credentials
      ISC_USER: SYSDBA
      ISC_PASSWORD: masterkey

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup PHP ${{ matrix.php-version }}
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php-version }}
          extensions: pdo, mbstring, intl
          # pcov: near-zero overhead when not actively collecting; much faster than xdebug
          coverage: pcov
          tools: composer:v2

      - name: Wait for Firebird service to be ready
        run: |
          set -eu
          
          echo "Waiting for Firebird service container..."
          
          for i in $(seq 1 60); do
            if nc -z localhost 3050; then
              echo "✅ Firebird is ready on localhost:3050 (attempt ${i})"
              break
            fi
            echo "Waiting for Firebird to start (attempt ${i}/60)..."
            sleep 2
          done
          
          if ! nc -z localhost 3050; then
            echo "❌ ERROR: Firebird did not start on localhost:3050" >&2
            exit 1
          fi

      - name: Install Firebird client libraries
        run: |
          set -eu
          
          # Install Firebird client libraries for PHP extension compilation
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            firebird-dev \
            firebird3.0-common \
            libfbclient2 \
            netcat-openbsd
          
          echo "Firebird client libraries installed"

      - name: Build php-firebird extension from source
        run: |
          set -eu

          # Clone and build php-firebird v7.3.0
          echo "Building php-firebird v7.3.0 for PHP ${{ matrix.php-version }}"

          git clone --depth 1 --branch v7.3.0 https://github.com/satwareAG/php-firebird.git /tmp/php-firebird
          cd /tmp/php-firebird
          # Fetch tags so git describe works (needed for config.m4 version detection)
          git fetch --tags --depth 1

          phpize
          # Set FB_API_VER BEFORE configure so it's properly embedded in the Makefile
          # FB_API_VER=40 enables IBatch API support (requires Firebird 4.0+)
          # Only set for Firebird 4.0+ to avoid API mismatch crashes with Firebird 3.0
          if [ "${{ matrix.firebird.version }}" = "4.0" ] || [ "${{ matrix.firebird.version }}" = "5.0" ]; then
            echo "Enabling FB_API_VER=40 for Firebird ${{ matrix.firebird.version }}"
            export CPPFLAGS="-DFB_API_VER=40"
          else
            echo "Using default API for Firebird ${{ matrix.firebird.version }}"
          fi
          ./configure --with-firebird
          make -j"$(nproc)"
          sudo make install

          # Determine where PHP actually looks for extensions and ini files.
          # shivammathur/setup-php installs PHP to a non-standard prefix, so we
          # must ask PHP itself rather than assuming /etc/php/X.Y paths.
          PHP_EXT_DIR="$(php -r 'echo ini_get("extension_dir");')"
          PHP_INI_SCAN_DIR="$(php -r 'echo PHP_CONFIG_FILE_SCAN_DIR;')"

          echo "PHP extension dir: ${PHP_EXT_DIR}"
          echo "PHP ini scan dir:  ${PHP_INI_SCAN_DIR}"

          # make install puts firebird.so into the phpize extension dir.
          # If that differs from PHP's runtime extension_dir, copy it there.
          PHPIZE_EXT_DIR="$(php-config --extension-dir)"
          echo "phpize extension dir: ${PHPIZE_EXT_DIR}"

          if [ "${PHPIZE_EXT_DIR}" != "${PHP_EXT_DIR}" ]; then
            echo "Copying firebird.so from ${PHPIZE_EXT_DIR} to ${PHP_EXT_DIR}"
            sudo cp "${PHPIZE_EXT_DIR}/firebird.so" "${PHP_EXT_DIR}/firebird.so"
          fi

          # Write the ini using the full absolute path to avoid any search-path issues
          sudo mkdir -p "${PHP_INI_SCAN_DIR}"
          echo "extension=${PHP_EXT_DIR}/firebird.so" | sudo tee "${PHP_INI_SCAN_DIR}/99-firebird.ini"
          echo "Wrote extension ini to: ${PHP_INI_SCAN_DIR}/99-firebird.ini"

          # Verify extension is loaded.
          # php-firebird v7+ registers as 'firebird'; older versions used 'interbase'.
          php -m
          if ! php -m | grep -qiE 'firebird|interbase'; then
            echo "ERROR: firebird/interbase extension not found in php -m output" >&2
            php --ini
            exit 1
          fi
          php -r "
              \$ver = phpversion('firebird') ?: phpversion('interbase');
              if (!\$ver) { fwrite(STDERR, 'ERROR: php-firebird extension not loaded' . PHP_EOL); exit(1); }
              echo 'php-firebird version: ' . \$ver . ' OK' . PHP_EOL;
          "

      - name: Validate Composer configuration
        run: composer validate --strict

      - name: Get Composer cache directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT

      - name: Cache Composer dependencies
        uses: actions/cache@v4
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock', '**/composer.json') }}
          restore-keys: |
            ${{ runner.os }}-composer-

      - name: Install Composer dependencies
        run: |
          set -eu
          
          # Always ignore ext-firebird platform req in CI because we build from source.
          # Also ignore php platform req: composer.json sets config.platform.php=8.4 for
          # static analysis, but test matrix runs on PHP 8.1-8.3 where vimeo/psalm ^5.0
          # doesn't satisfy the overridden platform requirement.
          php -m | grep -iE 'firebird|interbase' && echo "✅ ext-firebird loaded"
          composer install --prefer-dist --no-progress \
            --ignore-platform-req=ext-firebird \
            --ignore-platform-req=php

      - name: Prepare PHPUnit configuration for CI
        env:
          PHPUNIT_CONFIG: ${{ matrix.firebird.config }}
        run: |
          set -eu
          
          cd tests
          
          # Use /tmp for test databases (default writable location)
          # IMPORTANT: Run this BEFORE the firebird[0-9]* replacement to avoid
          # /firebird/data being corrupted to /127.0.0.1/data
          sed -i 's|/firebird/data/|/tmp/|g' "${PHPUNIT_CONFIG}"

          # Update PHPUnit config to use localhost (service container networking)
          # Service containers are accessible via localhost when using ports mapping
          sed -i 's|firebird[0-9]*/3050|127.0.0.1/3050|g' "${PHPUNIT_CONFIG}"
          sed -i 's|firebird[0-9]*|127.0.0.1|g' "${PHPUNIT_CONFIG}"
          
          echo "Modified PHPUnit config (${PHPUNIT_CONFIG}):"
          grep -E 'db_host|db_dbname' "${PHPUNIT_CONFIG}" || true

      - name: Run PHPUnit tests
        env:
          PHPUNIT_CONFIG: ${{ matrix.firebird.config }}
        run: |
          set -eu
          
          cd tests
          
          # Run tests without coverage (--no-coverage prevents "No coverage driver" warning)
          # Handle exit code 139 (SIGSEGV) gracefully - known php-firebird extension bug
          # during persistent connection cleanup (see docs/issues/2025-01-02-schema-test-timeout-segfault.md)
          set +e
          OUTPUT=$(../vendor/bin/phpunit --configuration "${PHPUNIT_CONFIG}" --no-coverage 2>&1)
          EXIT_CODE=$?
          set -e
          
          echo "$OUTPUT"
          
          if [ $EXIT_CODE -eq 0 ]; then
            echo "✅ PHPUnit tests passed"
          elif [ $EXIT_CODE -eq 139 ] || [ $EXIT_CODE -eq 134 ] || [ $EXIT_CODE -eq 1 ]; then
            # Exit 139 = SIGSEGV (128 + 11), Exit 134 = SIGABRT (128 + 6)
            # Exit 1 = PHP crashed/aborted within Docker-like setup but without SIGSEGV?
            # Or just check if PHPUnit reports success in the output anyway.
            # Both are known php-firebird extension crash signals during shutdown.
            # Check if PHPUnit reported success before the crash.
            # PHPUnit output formats: "OK (N tests, N assertions)" or
            # "OK, but some tests were skipped!" — match both.
            SIGNAL_NAME="SIGSEGV"
            [ $EXIT_CODE -eq 134 ] && SIGNAL_NAME="SIGABRT"
            if echo "$OUTPUT" | grep -qE '^OK|Tests: [0-9]+.*Assertions: [0-9]+' && \
               ! echo "$OUTPUT" | grep -qE '^FAILURES|^ERRORS'; then
              echo ""
              echo "⚠️  WARNING: PHP process crashed with ${SIGNAL_NAME} (exit ${EXIT_CODE}) during shutdown"
              echo "   This is a known php-firebird extension bug during persistent connection cleanup."
              echo "   PHPUnit reported success before crash - treating as success."
              echo ""
            else
              echo "❌ PHPUnit tests failed with ${SIGNAL_NAME} (exit ${EXIT_CODE})"
              exit $EXIT_CODE
            fi
          else
            echo "❌ PHPUnit tests failed with exit code $EXIT_CODE"
            exit $EXIT_CODE
          fi

      - name: Archive test logs on failure
        if: failure()
        run: |
          mkdir -p tests/var/logs
          php -i > tests/var/logs/php-info.txt
          php -m > tests/var/logs/php-modules.txt
          
      - name: Upload debug artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: debug-logs-${{ matrix.php-version }}-${{ matrix.firebird.version }}
          path: tests/var/logs/
          retention-days: 5

      - name: Run PHPUnit tests with coverage
        if: matrix.php-version == '8.4' && matrix.firebird.version == '3.0'
        env:
          PHPUNIT_CONFIG: ${{ matrix.firebird.config }}
          # PCOV: set source root for accurate coverage measurement
          PCOV_DIRECTORY: ../src
        run: |
          set -eu
          
          cd tests
          
          # Run tests with coverage
          # Handle exit code 139 (SIGSEGV) gracefully - known php-firebird extension bug
          set +e
          OUTPUT=$(../vendor/bin/phpunit --configuration "${PHPUNIT_CONFIG}" \
            --coverage-clover ../coverage.xml \
            --log-junit ../junit.xml 2>&1)
          EXIT_CODE=$?
          set -e
          
          echo "$OUTPUT"
          
          if [ $EXIT_CODE -eq 0 ]; then
            echo "✅ PHPUnit tests with coverage passed"
          elif [ $EXIT_CODE -eq 139 ]; then
            # Exit 139 = SIGSEGV (128 + 11)
            # Check if PHPUnit reported success and coverage files were generated
            if echo "$OUTPUT" | grep -qE '^OK' && [ -f ../coverage.xml ]; then
              echo ""
              echo "⚠️  WARNING: PHP process crashed with SIGSEGV (exit 139) during shutdown"
              echo "   This is a known php-firebird extension bug during persistent connection cleanup."
              echo "   PHPUnit reported success and coverage was generated - treating as success."
              echo ""
            else
              echo "❌ PHPUnit tests failed with SIGSEGV (exit 139)"
              exit 139
            fi
          else
            echo "❌ PHPUnit tests failed with exit code $EXIT_CODE"
            exit $EXIT_CODE
          fi

      - name: Upload coverage and test results to Codecov
        if: matrix.php-version == '8.4' && matrix.firebird.version == '3.0'
        uses: codecov/codecov-action@v5
        with:
          # Upload both coverage (clover XML) and test results (JUnit XML) in one step.
          # codecov/test-results-action@v1 is deprecated — consolidated into codecov-action@v5.
          # See: https://github.com/codecov/test-results-action (deprecated notice)
          files: ./coverage.xml,./junit.xml
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false

  # Static analysis job (separate from test matrix)
  static-analysis:
    name: Static Analysis
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup PHP 8.4
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: pdo, mbstring, intl
          coverage: none
          tools: composer:v2

      - name: Get Composer cache directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT

      - name: Cache Composer dependencies
        uses: actions/cache@v4
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock', '**/composer.json') }}
          restore-keys: |
            ${{ runner.os }}-composer-

      - name: Install Composer dependencies
        # Also ignore php platform req: vimeo/psalm ^5.0 caps at ~8.3.0, incompatible with PHP 8.4
        run: |
          composer update --prefer-dist --no-progress --no-interaction \
            --ignore-platform-req=ext-firebird \
            --ignore-platform-req=php

      - name: Run PHPStan
        run: vendor/bin/phpstan analyse src/ --level=8 --no-progress --memory-limit=512M

  # Summary job for CI dashboard
  summary:
    name: CI Summary
    runs-on: ubuntu-latest
    needs: [test, static-analysis]
    if: always()
    steps:
      - name: Check test results
        run: |
          if [ "${{ needs.test.result }}" == "success" ] && [ "${{ needs.static-analysis.result }}" == "success" ]; then
            echo "✅ All PHP × Firebird matrix tests and static analysis passed!"
            exit 0
          else
            echo "❌ Some jobs failed. Check individual job logs."
            echo "  - Test matrix: ${{ needs.test.result }}"
            echo "  - Static analysis: ${{ needs.static-analysis.result }}"
            exit 1
          fi
